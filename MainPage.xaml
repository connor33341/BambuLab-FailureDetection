<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BambuLab_FailureDetection.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Bambulab Failure Detection"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Version 1.2"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="dotnet" />

            <Button
                x:Name="SetupButton"
                Text="Setup/Configure" 
                SemanticProperties.Hint="dotnet"
                Clicked="SetupButton_Clicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ViewButton"
                Text="View" 
                SemanticProperties.Hint="dotnet"
                Clicked="ViewButton_Clicked"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
